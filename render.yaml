services:
  - type: web
    name: meva-medical-api
    env: node
    root: backend/API
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: PORT
        value: 10000
